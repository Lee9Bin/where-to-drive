<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gyub.WhereToDrive.member.dao.MemberDAO">
	<!-- 게시판 글 작성 -->
	<insert id="register">
	    INSERT INTO MP_MEMBER(   memberId 
	   					       , memberPw 
	   					       , memberName
	   					       , memberMail
	   					       , memberAddr1
	   					       , memberAddr2	)
	                  VALUES(    #{memberId} 
	                 		   , #{memberPw}
	                 		   , #{memberName}
	                 		   , #{memberMail}
	                 		   , #{memberAddr1}
	                 		   , #{memberAddr2})
    </insert>
   <!-- 로그인 -->
	<select id="login" resultType="com.gyub.WhereToDrive.member.entity.MemberVO">
		SELECT memberId, memberPw 
		  FROM MP_MEMBER
		 WHERE memberId = #{memberId}
		   AND memberPw = #{memberPw}   
	</select>

	<!-- 아이디 중복 체크 -->
	<select id="idChk" resultType="int">
		SELECT COUNT(*) FROM MP_MEMBER
		WHERE memberId = #{memberId}
	</select>
</mapper>